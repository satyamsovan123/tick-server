const { logger } = require("./logger");
const {
  getServerDetails,
  overengineedBoxifier,
} = require("./getServerDetails");
module.exports = {
  logger,
  getServerDetails,
  overengineedBoxifier,
};
